# 手机与电脑安装使用说明

文档日期：2026-09-13。对应 Android 0.3.1 和包含图标、COM3 优先选择及目录监测功能的 Windows 接收端源码。

## 一、开始前先选择版本

| 设备或系统 | 应使用的版本 | 注意事项 |
| --- | --- | --- |
| 安卓手机 Android 6.0 及以上 | Android APK | 同一安装包，不必按手机 32/64 位分别下载；蓝牙能力需真机确认 |
| Win7 32 位 | Win7-x86 接收器 | 不能使用 x64 EXE；建议在 Win7 SP1 环境验证 |
| Win7 64 位 | 可尝试 Win7-x86 接收器 | 没有独立验证的 Win7 x64 发布版本 |
| Win11 Intel/AMD 64 位 | Win11-x64 接收器 | 使用专用 x64 编译入口，不要因文件名相同而混用不同目录的 EXE |
| Win11 ARM64 | 暂不提供专用版本 | 当前 x64 构建入口不面向 ARM64，不承诺模拟运行兼容性 |
| Win8 / Win8.1 / Win10 | 尚未列入验证矩阵 | 不应把相近系统当作已验证支持 |
| iPhone / macOS | 不提供当前客户端 | APK 不能安装在 iPhone，Windows EXE 不是 Mac 程序 |

### Windows 环境区别

接收器源项目目标是 .NET Framework 4.0，运行需要兼容的 .NET Framework 4.x；本地一键编译还需要其 `csc.exe`。安装现代 .NET 运行时并不等于满足这一要求。

Win7 上需要检查 SP1、补丁和 .NET 环境；例如 .NET Framework 4.8 的系统要求包含 Win7 SP1，而不能直接套用 Win11 的组件安装方案。Win11 通常已有 .NET Framework 4.x，应先检查，不要尝试降级系统组件。具体要求以 [微软 .NET Framework 系统要求](https://learn.microsoft.com/en-us/dotnet/framework/get-started/system-requirements) 和 [官方安装指南](https://learn.microsoft.com/en-us/dotnet/framework/install/) 为准。

Win7 已结束支持。保留兼容入口不代表其系统安全得到保障；涉及内网电脑时，请由单位管理员确认部署方式。[微软支持说明](https://learn.microsoft.com/en-us/troubleshoot/windows-client/windows-7-eos-faq/windows-7-end-support-faq-general)

## 二、安卓手机安装 APK

1. 下载发布者提供的 APK。当前文件名为 `remote-med-writer-android-0.3.1-debug.apk`。
2. 将 APK 保存到手机，用“文件管理”找到并打开。
3. 如果系统要求允许当前来源安装应用，确认文件来源可信后，按提示仅授权当前使用的文件管理器或浏览器。安装结束后可收回该权限；不要关闭系统安全保护。
4. 安装完成后，打开“蓝牙文字传输”。图标为带“懒”字的医疗文件夹。
5. 界面顶部有“文字传输”和“文件传输”两个标签。设备和连接方式在折叠设置中。

### 权限、更新和限制

- 文字连接需要手机蓝牙已开启；Android 12 及以上按提示允许“附近设备”权限。
- 文件通过系统文件选择器选取，不需要授予整个手机存储空间的访问权限。
- 当前版本没有内置 OCR；先在其他应用识别，再粘贴结果。
- 当前 APK 为测试签名。相同包名、相同签名的后续版本可以尝试覆盖安装；签名不同可能报安装冲突。不要贸然卸载，应先保存需要的内容。
- 切换标签页会保留当前内容；退出、旋转屏幕或系统回收应用可能中断文字传输或丢失正文，请保持前台并保留原始文字。
- 如果系统安全机制阻止安装，不要关闭防护；检查来源、签名和具体错误，必要时联系管理员。

## 三、电脑端：运行 BAT 脚本构建并启动

**当前 Windows 下载包没有预编译 EXE，也没有 EXE 安装程序。启动入口是 BAT 批处理脚本，包中同时包含构建所需的 C# 源码和图标。** BAT 不是独立接收器，不能只下载或复制一个 BAT 使用。

1. 下载并完整解压源码包到本地可写目录，例如 `C:\BTReceiver`，不要直接在压缩包中运行。若下载的是 `bluetooth-transfer-assistant-mit-source.zip`，进入解压后的 `github-release-source\windows` 目录；确保 `.cs` 文件、`receiver.ico`、`Properties` 文件夹和 `BUILD-RECEIVER.bat` 等脚本在一起。先关闭旧接收器及占用同一 COM 端口的 PowerShell 脚本或串口工具。
2. 根据电脑系统双击：

| 系统 | 双击入口 | 成功后生成位置 |
| --- | --- | --- |
| Win7 32 位 | `BUILD-AND-RUN-WIN7-X86.bat` | `bin\Win7-x86\RemoteMedWriterReceiver.exe` |
| Win11 Intel/AMD 64 位 | `BUILD-AND-RUN-WIN11-X64.bat` | `bin\Win11-x64\RemoteMedWriterReceiver.exe` |

3. 首次双击上述 BAT，脚本会检查 .NET 编译器，在电脑本地生成对应位数的 EXE，成功后自动启动接收器。这些 EXE 是运行 BAT 后生成的，不是下载包自带的文件。后续可再次运行 BAT，或直接打开已生成的 EXE。
4. 编译失败会保留错误窗口，日志在相应输出目录的 `build.log`。请截图或提供脱敏日志。
5. 如果提示无法替换 EXE，先关闭正在运行的旧接收器，再重试。脚本不会强制结束进程。
6. 如需桌面入口，可为生成的 EXE 创建快捷方式。自定义图标用于生成的程序及其窗口；BAT 文件本身通常显示系统批处理图标。

32 位编译器通常检查路径：`C:\Windows\Microsoft.NET\Framework\v4.0.30319\csc.exe`。实际 Windows 安装位置由脚本读取，无需手工修改。无需 Visual Studio，但必须具备脚本使用的编译器和程序集。

## 四、文字传输：收到后自动打开记事本

1. 在手机和电脑的系统蓝牙设置中完成配对，核对电脑名称。
2. 在 Windows 蓝牙设置里确认支持 SPP 的“传入 COM”端口。不要把传出串口、其他设备串口当成接收口。
3. 打开接收器。存在 COM3 时默认优先选择 COM3；不存在时需要选择实际端口。默认选中不等于已经开始监听。
4. 勾选“接收后自动打开记事本”，点击“开始监听”。
5. 手机选择“文字传输”，展开“接收设备与连接方式”，选择“经典蓝牙串口（电脑 SPP）”，选择或填写这台电脑的蓝牙地址。
6. 输入或粘贴脱敏文字，点击“连接并发送文字”，核对目标后确认。
7. 完整接收后电脑回复 ACK，并尝试新开记事本显示正文。手机的完整接收确认不保证记事本一定已经打开，请同时检查电脑状态栏。

如果使用配套 ESP32 BLE 串口桥：电脑选择桥接器 COM，手机选择 BLE 模式。接收器为 115200、8N1、无流控，需要与桥接器固件一致；不要直接沿用旧脚本的 9600。

Win7 与 Win11 的记事本窗口结构可能不同。接收器优先采用 Unicode 窗口写入，不模拟键盘；写入不可用时生成 UTF-8 BOM 临时 TXT，再交给记事本打开。Win11 的具体表现需真机验证。

## 五、文件传输与自动打开文件夹

1. 在电脑创建一个专用接收目录，例如 `C:\BTReceived`。
2. 在接收器点击“选择文件保存目录”，选择此目录，勾选“监测新文件并打开文件夹”。监测默认关闭，每次启动需确认目录并启用。
3. 在 Windows 系统蓝牙功能中打开“接收文件”。这一步与 COM 监听独立。
4. 手机切换到“文件传输”，选择本地文件，点击分享并确认。
5. 在手机系统分享面板选择“蓝牙”，再选择目标电脑。当前版本尚未实现自动跳过这些选择步骤。
6. 在电脑完成系统接收流程，将文件保存到第 2 步选择的目录。
7. 接收器发现新建或更新文件，大小和修改时间稳定约 6 秒且文件可独占打开后，会打开保存目录；不会自动运行文件。

监测只看指定目录顶层，不监测子目录。启动前已有文件不触发；`.tmp`、`.part`、`.crdownload` 被忽略。其他软件写入同一目录也可能触发，长时间暂停写入也可能被误判为稳定。因此它不是系统蓝牙完成通知，建议用小规模专用目录。

文件分享只保存文件，不会自动把文件内容插入记事本。需要直接显示文字时，请使用文字传输标签。

## 六、常见问题

| 现象 | 检查方法 |
| --- | --- |
| COM3 不存在 | 选择实际传入 COM，检查电脑是否支持 SPP |
| COM 被占用 | 关闭旧脚本、旧接收器或其他串口工具 |
| 能发文件但不能发文字 | 文件分享和 SPP 是不同通道，确认传入 COM 服务 |
| APK 等待确认超时 | 确认电脑使用配套接收器，而非旧裸文本 PowerShell 脚本 |
| 文字乱码或带 RMW/END 字样 | 确认使用本项目匹配协议的接收器 |
| 接收后没有记事本 | 查看电脑状态栏，确认程序在当前桌面会话运行 |
| 文件夹没有自动打开 | 确认监测已启用、路径与系统最终保存路径一致，且文件不在子目录 |
| 文件夹打开过早 | 稳定时间是启发式判断，先确认系统传输结束再操作文件 |
| 系统分享里没有蓝牙 | 检查手机系统是否提供蓝牙文件分享；不要误选云盘 |
| 下载包里没有 EXE | 这是正常情况；完整解压后运行对应系统的 BAT，编译成功才会生成 EXE |
| BAT 提示找不到编译器 | 请管理员检查 .NET Framework 4.x 的 csc.exe；不要只安装现代 .NET 运行时 |
| BAT 编译失败 | 查看对应 bin 子目录的 build.log，提供脱敏后的日志；不要只复制 BAT 或在压缩包里运行 |
| 本地生成的 EXE 提示无法运行 | 核对 x86/x64、Windows 架构、.NET 环境及包是否完整 |
| 图标还是旧的 | 从新 EXE 重新创建快捷方式；不要修改注册表来强制清缓存 |

## 七、首次验收与数据清理

先发送两行测试文字：

```text
中文测试：你好，Windows！
第二行：温度 36.5℃，数字 123456。
```

确认中文、换行和手机 ACK 正常，再测试一个无个人信息的小文件。中断后重试前先查看电脑是否已收到，避免重复；停止监听再开始可清除半包缓存，退出程序则会丢失会话内去重记录。

记事本回退文件保存在 `%TEMP%\RemoteMedWriter`，不会自动删除。剪贴板、手动保存和蓝牙接收文件也由用户管理。关闭接收器会停止监听和目录监测；不要为了使用本工具关闭防火墙、安全软件或绕过单位内网限制。
